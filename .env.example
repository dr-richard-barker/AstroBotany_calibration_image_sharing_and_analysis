# AstroBotany Calibration Image Database — configuration
#
# There is NO Google GenAI / Gemini key here anymore. Marker detection and
# colour calibration run entirely client-side; storage is local (node:sqlite).

# Port for the Express + node:sqlite server (default 3000).
PORT=3000
